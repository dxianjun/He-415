# 开发规范

## 编码规则
- C 语言项目文件：统一使用 ANSI（GBK）编码。
- Python、Java 及其他编程语言项目：统一使用 UTF-8 (no BOM) 编码。
- Markdown 文档（`.md`）：统一使用 UTF-8 (no BOM) 编码。

## 执行要求
- 新建文件时必须按上述编码创建。
- 修改已有文件时，不得改变该项目规定编码。
- 禁止在同一项目中混用多种文本编码。

## 提交前检查
- 提交前确认新增/修改文件编码符合规则。
- 若发现乱码，先修复编码再提交。
